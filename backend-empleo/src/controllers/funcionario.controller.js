const funcionarioCtrl = {};

//methods all get all query
funcionarioCtrl.getConsultaPostulados = (req,res)=>res.json({'message':'ruta postulado'});

//method generate report (post)
funcionarioCtrl.generateReport = (req,res)=>res.json({'message':'ruta postulado'});

//method get unique
funcionarioCtrl.getConsultaPostulado = (req,res)=>res.json({'message':'ruta postulado'});

//method update postulado
funcionarioCtrl.updateInfoPostulado = (req,res)=>res.json({'message':'ruta postulado'});

module.exports = funcionarioCtrl;